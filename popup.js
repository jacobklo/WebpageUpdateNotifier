var globalData = chrome.extension.getbackgroundpage().globalData;
var JsonAction = chrome.extension.getbackgroundpage().JsonAction;

for (var i in globalData.websites) {
  var web = globalData.websites[i];
  
}